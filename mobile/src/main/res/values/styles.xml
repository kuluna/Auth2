<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/brandcolor_primary</item>
        <item name="colorPrimaryDark">@color/brandcolor_dark</item>
        <item name="colorAccent">@color/brandcolor_accent</item>
    </style>

</resources>
